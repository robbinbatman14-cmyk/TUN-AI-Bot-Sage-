# Knowledge Folder

This folder is just a scratch space if you want to keep source copies of
your documents locally for your own records.

It is **not** read automatically by the bot. Documents actually enter
UNAI's knowledge base through the `/knowledge upload` Discord command,
which stores them in the database and walks them through the approval
workflow described in the README (pending → approve → indexed).
